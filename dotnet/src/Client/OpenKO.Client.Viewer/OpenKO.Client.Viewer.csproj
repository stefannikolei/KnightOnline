<Project Sdk="Microsoft.NET.Sdk">

  <!-- Debug viewer: renders real Client/Data assets scene by scene (stage 6). -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\OpenKO.Client.Engine\OpenKO.Client.Engine.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Bundled UI fonts (Noto Sans / Noto Sans KR, SIL OFL — see OFL.txt). -->
    <Content Include="Assets\Fonts\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

</Project>
